import numpy as np
from collections import Counter

# a = [[1, 2, 3],[4, 2, 6]]
# arr = np.array(a)
# # size=arr.size
# # shape=arr.shape
# res=Counter(arr.flatten())
# a=res.keys()

a=np.zeros((3,3),dtype=float)
print(a)